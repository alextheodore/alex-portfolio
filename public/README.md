# Drop your assets here

- `portrait.jpg`  — professional portrait (hero). Falls back to a gradient monogram if absent.
- `resume.pdf`    — linked from nav + hero.
- `og.png`        — 1200×630 social share image.
- `projects/<slug>/cover.jpg` etc. — optional project gallery images.
